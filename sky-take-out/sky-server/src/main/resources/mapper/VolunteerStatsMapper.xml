<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.VolunteerStatsMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into volunteer_stats (user_id, total_orders, total_hours, rating, level, create_time, update_time)
        values (#{userId}, #{totalOrders}, #{totalHours}, #{rating}, #{level}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        update volunteer_stats
        <set>
            <if test="totalOrders != null">total_orders = #{totalOrders},</if>
            <if test="totalHours != null">total_hours = #{totalHours},</if>
            <if test="rating != null">rating = #{rating},</if>
            <if test="level != null">level = #{level},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>
</mapper>
